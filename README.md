Used to enable extra content/features https://github.com/aiwag/coomerlab Desktop app

1) Bun

linux/mac
`curl -fsSL https://bun.sh/install | bash`
win
`powershell -c "irm bun.sh/install.ps1 | iex"`

3) Run

build binary (docs - https://bun.com/docs/bundler/executables)
`bun build server.ts --compile --outfile javlab-server`
